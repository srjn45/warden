<section class="section">
  <div class="wrap" style="display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:14px">
    <div style="background:var(--panel);border:1px solid var(--border);border-radius:8px;padding:16px">
      <h3>🧭 One cockpit</h3><p style="color:var(--muted)">TUI + web. See every agent's status at a glance.</p></div>
    <div style="background:var(--panel);border:1px solid var(--border);border-radius:8px;padding:16px">
      <h3>🔗 Pipelines</h3><p style="color:var(--muted)">Chain dependent agents into a DAG — analyze → implement → review.</p></div>
    <div style="background:var(--panel);border:1px solid var(--border);border-radius:8px;padding:16px">
      <h3>💸 Measured savings</h3><p style="color:var(--muted)">A real ledger of tokens kept out of context, plus per-model cost governance.</p></div>
    <div style="background:var(--panel);border:1px solid var(--border);border-radius:8px;padding:16px">
      <h3>📦 One binary</h3><p style="color:var(--muted)">Daemon, CLI, TUI, web, MCP. No database.</p></div>
  </div>
</section>
